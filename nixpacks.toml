providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.build]
cmds = ["npm ci", "npx prisma generate --schema server/prisma/schema.prisma", "npm run build"]

[start]
cmd = "npm run start"
