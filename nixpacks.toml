providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate --schema server/prisma/schema.prisma", "npm run build"]

[start]
cmd = "npm run start"
